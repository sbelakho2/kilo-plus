{
  "env_var": "FAKTOR_SERVER_PASSWORD",
  "secret_format": "64 hex chars, generated by the frontend, passed to the daemon via env; the daemon never prints it",
  "basic_auth": "Authorization: Basic base64(\"kilo:\" + FAKTOR_SERVER_PASSWORD) — the frozen v7.5.6 form, required on every endpoint including /global/health (see basic_auth.json)",
  "authorization_bearer": "Authorization: Bearer <password>",
  "x_faktor_server_password": "x-kilo-server-password: <password>",
  "accepted_header_forms": ["authorization_basic", "authorization_bearer", "x_faktor_server_password"],
  "public_endpoints": [],
  "legacy_public_alias": "/api/hello",
  "unauthorized": {
    "code": "unauthorized",
    "http_status": 401,
    "retryable": false
  }
}
